name: CI Pipeline

on:
  push:
    branches:
      - dev
      - develop
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Télécharger le code
        uses: actions/checkout@v3

      - name: Vérifier le contenu du repo
        run: ls -la  # Vérifie les fichiers clonés

      - name: Installer Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      # 🔹 Installation & Tests du Back-Office
      - name: Installer les dépendances du Back-Office
        run: |
          cd frontend/back-office
          npm install

      - name: Lint du Back-Office
        run: |
          cd frontend/back-office
          npm run lint

      - name: Tests du Back-Office
        run: |
          cd frontend/back-office
          npm test

      # 🔹 Installation & Tests du Front-Office
      - name: Installer les dépendances du Front-Office
        run: |
          cd frontend/front-office
          npm install

      - name: Lint du Front-Office
        run: |
          cd frontend/front-office
          npm run lint

      - name: Tests du Front-Office
        run: |
          cd frontend/front-office
          npm test
